pipeline {
   agent any

   stages {
	stage('install req') {
		steps {
	  		sh 'pip install requirements'
		}
	}
      stage('run tests') {
        steps{      
          sh 'python tests.py'
        }
        }

  }
}
